[edit]
{{ if lookPath "cursor" -}}
command = "cursor"
{{- else if lookPath "code" -}}
command = "code"
{{- end }}
args = ["--wait"]
[diff]
command = "delta"
pager = "delta"
format = "git"
args = ["--paging=never"]
exclude = ["dirs"]